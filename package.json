{
  "name": "family-fund",
  "version": "2.0.0",
  "private": true,
  "description": "Fondo Familiar — interactive tracker with per-sibling deposit entry",
  "dependencies": {
    "@netlify/blobs": "^8.1.0",
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "playwright": "^1.62.0"
  }
}
